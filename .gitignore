# Claude Code session data
.claude/

# Binary assets not needed in version control
*.png
*.jpg
*.jpeg

# Plex configuration — contains live auth token (PlexOnlineToken); never commit
Preferences.xml
